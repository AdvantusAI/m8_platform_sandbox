import { _ModuleSupport, _Scene } from 'ag-charts-community';
import { BaseFunnelSeries, type Bounds, type FunnelAnimationData, type FunnelNodeDatum, type FunnelNodeLabelDatum, type FunnelSeriesShapeStyle } from './baseFunnelSeries';
import { FunnelProperties } from './funnelProperties';
export declare class FunnelSeries extends BaseFunnelSeries<_Scene.Rect> {
    static readonly className = "FunnelSeries";
    static readonly type: "funnel";
    properties: FunnelProperties;
    constructor(moduleCtx: _ModuleSupport.ModuleContext);
    getBandScalePadding(): {
        inner: number;
        outer: number;
    };
    protected connectorEnabled(): boolean;
    protected barStyle(): FunnelSeriesShapeStyle;
    protected connectorStyle(): FunnelSeriesShapeStyle;
    protected nodeFactory(): _Scene.Rect;
    protected createLabelData({ rect, yDatum, datum, }: {
        rect: Bounds;
        barAlongX: boolean;
        yDatum: number;
        datum: any;
    }): FunnelNodeLabelDatum | undefined;
    protected updateDatumNodes(opts: {
        datumSelection: _Scene.Selection<_Scene.Rect, FunnelNodeDatum>;
        isHighlight: boolean;
    }): Promise<void>;
    animateEmptyUpdateReady(params: FunnelAnimationData<_Scene.Rect>): void;
    animateWaitingUpdateReady(data: FunnelAnimationData<_Scene.Rect>): void;
}
