import { _ModuleSupport } from 'ag-charts-community';
export declare function snapToAngle({ x, y }: _ModuleSupport.Vec2, center: _ModuleSupport.Vec2, step: number, direction?: number): {
    x: number;
    y: number;
};
